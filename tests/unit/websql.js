/* global define */
define((require) => {
  const registerSuite = require('intern!object');
  const assert = require('intern/chai!assert');
  const websql = require('../../src/websql');

  registerSuite({
    websqlPromise: 'hello'
  })
});
